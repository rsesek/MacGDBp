{
    IBClasses = (
        {
            ACTIONS = {connect = id; }; 
            CLASS = ConnectWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {"_port" = NSTextField; "_session" = NSTextField; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}